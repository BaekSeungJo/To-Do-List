spring:
  datasource:
    url: jdbc:tc:postgresql:14:///todo
    username: todo
    password: todo
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
    show-sql: false
  flyway:
    enabled: true
    locations: classpath:db/migration
